syntax = "proto3";

message DataMessage {
  int32 id = 1;
  string payload = 2;
  repeated int32 values = 3;
}

message Ack {
  string message = 1;
}

service GRPCDemo {
  rpc StreamData (stream DataMessage) returns (Ack);
}

